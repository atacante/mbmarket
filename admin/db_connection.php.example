<?php

$servername = "localhost";
$username = "root";
$password = "";

// Create connection
$conn = mysqli_connect($servername, $username, $password, 'mbmarket');

// Check connection
if (!$conn) {
    echo "Connection failed: " . mysqli_connect_error();
    exit;
}

?>